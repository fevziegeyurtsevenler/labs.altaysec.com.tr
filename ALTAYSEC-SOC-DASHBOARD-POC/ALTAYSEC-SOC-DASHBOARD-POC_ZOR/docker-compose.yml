version: '3.8'

services:
  altaysec-siem-lab-hard:
    build: .
    container_name: altaysec-soc-level3
    ports:
      - "5001:5000"
    restart: always
    environment:
      - FLASK_ENV=production