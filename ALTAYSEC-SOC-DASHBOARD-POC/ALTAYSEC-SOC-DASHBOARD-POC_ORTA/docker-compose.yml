version: '3.8'

services:
  altaysec-siem-lab-medium:
    build: .
    container_name: altaysec-soc-level2
    ports:
      - "5001:5000"
    restart: always
    environment:
      - FLASK_ENV=production